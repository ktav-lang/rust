# Checkpoint — 2026-09-08 12:45

## Session summary

Long-running session driving the `ktav` Rust crate (`D:\dev\ktav-lang\rust`) toward spec 0.7.0 conformance through a repeating loop: an external process drops an independent review report into `docs/reviews/`, the user pastes a short summary and says "давай сделаем исправления по ревью с помощью rush", and I decompose the findings into a `/babygoal` TaskList and run them as isolated `/wrush` worktree agents — in parallel when their file sets are disjoint, sequentially when not. Every sub-agent result is zero-trust verified by me personally: I read the whole diff, re-run `cargo build/test/clippy/fmt` with `KTAV_SPEC_DIR` pointed at the primary checkout's initialized spec submodule, regenerate the golden corpus on BOTH sides myself rather than trusting the agent's, and re-run any performance or allocation measurement independently before merging by `git cherry-pick` and removing the worktree.

Rounds 5, 6 and 7 were all completed in this session. Round 5 (5 findings) and round 6 (9 findings: 1 P1, 4 P2, 4 P3) are fully closed. Round 7 (4 P2 + 2 P3, report at `docs/reviews/2026-09-08-0856-spec-0.7-rust-review-round-7.md`, commit `53c879d`) is also fully closed — notably round 7 reviewed work merged EARLIER IN THIS SAME SESSION and its criticisms were fair: R7-F1 showed that A2 step 1 unified the scanner loop but NOT the semantics of its configs (a frozen "quirk" const `OPENER_GATED=false` was actually a latent bug that rejected the valid document `[{a: x[, "b}c": 2}]`), and R7-F2 showed the public docs I merged hours earlier claimed "no intermediate tree" while the new inline path built a `Node` DOM. Both are now fixed.

One push happened this session, explicitly authorized: `0b5aac2..aeab4c8`, 67 commits, CI green. **22 commits are currently unpushed** and the user has been asked whether to push them; no answer yet.

In flight right now: task #161, a single `/wrush` agent (`rust-rescan`, session alive, currently delegating to a sub-agent) attacking the structural root cause of a recurring deep-nesting slowdown. It runs ALONE on purpose because its acceptance criterion is a wall-clock measurement and this machine only resolves to about ±10% when quiet (2.4x swings under load).

Two hard-won measurement lessons are now baked into every task description, because both bit me personally: (1) the `bench_ab` harness CALIBRATES iteration counts per binary and prints `SCEN <name> iters=<n>`, so raw batch nanoseconds are NOT comparable across binaries — I once computed "47% faster" from un-normalized data when the truth was "7% slower"; (2) wall-clock on this machine is only good to ±10%, so deterministic instruments (allocation counters, event-scan counters, `Bump::allocated_bytes()`) are strongly preferred, and a counting allocator must forward all four `GlobalAlloc` ops to `System` or it silently substitutes the realloc path (fixed this session in commit `b9b5320`).

The golden-corpus harness lives OUTSIDE the repos at `D:/system_artefact/Temp/ktav-a2-harness/` — `a2-step1-harness/` (post side) and `a2-step1-harness-base/` (base side), each with a `Cargo.toml` whose `ktav` path dep must be re-pointed before use, and whose stored `baseline_corpus.txt` is STALE (the corpus grows as tests are added: 7400 → 7422 → 7461 inputs over this session). Both sides must always be regenerated fresh; both `corpus_dump.rs` copies must harvest from the same tree or the input sets differ.

A subtle trap discovered late: an EMPTY corpus diff does not prove a behavioral change is absent. Task #162 changed VT dedent behavior and the corpus diff was still empty, because only 2 of 7461 inputs contain VT at all and neither in a leading run. The agent correctly built an explicit positive-control probe instead of declaring victory — that pattern is now required in task #163's description.

## Active goal

None. `/goal` was never armed; progress is governed by the TaskList plus the `/babysit` cron, per the `/babygoal` contract.

## TaskList

### in_progress
- #161 Eliminate the per-level inline re-scan (root cause of the recurring deep-nesting cost)

### pending
- #163 Multi-byte § 3.3 whitespace does not participate in the multiline dedent (spec deviation)

### recently completed (most recent 10)
- #162 Decide the VT (U+000B) question in the multiline-dedent byte scans
- #156 A3 follow-up: canonical_float computed outside `if strict` on the multi-line path
- #154 Finish the § 3.3 whitespace migration: 57 remaining host-delegating sites
- #157 R7-F1 (P2): unify opener/raw/quoted-key SEMANTICS across scanner configs, not just the loop
- #158 R7-F3 (P2): single-pass inline path registration, drop the O(D²) re-scans
- #159 R7-F2 (P2): the inline path still builds a Node tree while the public docs deny it
- #160 R7-F4 (P2): make the measuring allocators transparent (both of them)
- #153 A2 step 2: thin path emits inline events directly (drop the owned DOM) + fix the public docs
- #155 Close the deep-nesting regression left by the scanner unification (+7…+9%)
- #152 A2 step 1: unify the three inline scanners into one boundary/scope model

No deleted tasks.

## Decisions

- **Merged R7-F1 despite a measured 12–19% deep-nesting regression** (my own pinned measurement: deep_nest 1.127, deep_nest_arr 1.189, deep_nest_events 1.155; mainstream 0.96–0.99). Rejected the alternative of holding it: it fixes a SPEC VIOLATION — both public APIs rejected the valid document `[{a: x[, "b}c": 2}]` — and the added cost is `find` finally doing the minimum correct work. Opened #161 to fix the structural cause instead of reverting the correctness fix. This differs from the earlier #155 case, where the regression came from a pure refactor with no user-visible benefit.
- **Accepted R7-F2's docs-first, structure-second split.** The agent corrected the false public claims in a separate commit, then removed only the ARRAY intermediate `Vec<Node>`, and argued (correctly, I checked) that object staging is load-bearing: dotted re-entry like `{a.b: 1, c: 2, a.d: 3}` must emit `a`'s block grouped, and `EventSink` has no rollback under the atomic-publication constraint I imposed. Rejected forcing a full DOM removal to make the docs prettier.
- **Chose path (a) for the VT question (#162)** — VT now participates in the dedent — on spec text I verified verbatim: § 5.6 measures the common leading run "in whitespace code points (§ 3.3) rather than bytes", and § 3.3 states "There is no separate, narrower 'structural' whitespace concept". Rejected path (b) (keep the narrower ASCII class) because it would institutionalize exactly the concept the spec denies exists.
- **Kept the A1 conclusion "arena reservation policy stands"** after R7-F4 corrected the allocator: its reserved-vs-used figures came from `Bump::allocated_bytes()`, which is allocator-independent, and I re-verified the table byte-identical. Only the secondary allocation-count table changed meaning (old "allocs" were alloc+realloc+zeroed conflated; the new per-op numbers decompose the old ones exactly — 138+9+6 = 153, verified by me).
- **Ran #161 alone rather than in parallel with #163**, despite disjoint file sets, because #161's acceptance is a wall-clock measurement and concurrent agents demonstrably poison this machine's timings.

## Open questions

- **Push or not?** 22 commits are unpushed on `rust/main` (`origin/main` is at `aeab4c8`, local HEAD `4477ae2`). I asked the user at the end of the last report; no answer yet. Pushing commits is safe — `release.yml` fires only on `v*` tags, `ci.yml` runs lint/tests on push to main — but per standing rules a push needs explicit authorization each time.
- Whether #161 can reach parity at all is genuinely unknown. Its prompt explicitly accepts "here is the wall, with numbers" as a valid deliverable rather than forcing a restructure that would violate byte-identical behavior or re-fork the scanner configs.

## Repo state

**rust** (`D:\dev\ktav-lang\rust`) — working tree clean:
```
(clean)
```
```
4477ae2 test(de): pin VT dedent behavior on both parsers (§ 3.3 × § 5.6)
0f395f0 fix(ws): route multiline dedent leading-run scan through the § 3.3 byte view
1f8ca08 docs(example): single-line doc bullets for clippy doc_lazy_continuation
cf092d0 test(strict): pin multi-line float LossyScalar payload and lax Ryu forms
62c5457 perf(classify): compute canonical_float only under strict (R7-P3)
```
Ahead of `origin/main` by **22 commits**. `Cargo.toml` `version` and `package.metadata.ktav.spec-version` both still `0.6.4` throughout the entire session (rule #13 honored). Suite at this HEAD: **1036 tests / 51 suites, 0 failures**; clippy `-D warnings` and `cargo fmt --check` clean. Spec submodule unchanged at `7f05a1b`.

Live worktrees (three are scaffolding for #161's A/B and corpus, to be removed when it lands):
```
D:/dev/ktav-lang/rust                       4477ae2 [main]
D:/dev/ktav-lang/rust/worktrees/rust-rescan 4477ae2 [rust-rescan]   ← #161 agent, ACTIVE
D:/dev/ktav-lang/rust/worktrees/rescan-cur  4477ae2 (detached)      ← corpus/A-B base = today's main
D:/dev/ktav-lang/rust/worktrees/rescan-base a74b302 (detached)      ← A/B target = pre-R7-F1
D:/dev/ktav-lang/rust/worktrees/p3-base     62c5457 (detached)      ← leftover from #156/#162, removable
D:/system_artefact/Temp/ktav-pr4            f89be72 [pr-4-v2-local] ← pre-existing, not mine
```

**Cron:** one babysit job armed, `3f9fc5e0`, `7,22,37,52 * * * *` (every 15 min, session-only). A duplicate from the round-6 chain (`cdd830d7`) was deleted earlier — it had never self-removed because the TaskList never emptied.
