# Checkpoint — 2026-09-09 13:27

## Session summary

Continuation of the long-running review-fix loop for the `ktav` Rust crate (`D:\dev\ktav-lang\rust`) against spec 0.7.0. Since the last checkpoint (`2026-09-08-2116.md`, which closed round 8), this session closed **rounds 9, 10, and 11** of the external reviewer's report series via the established `/babygoal` → `/wrush` isolated-worktree pattern, then did something new: at the user's explicit direction, audited the *entire* review-round history plus the pre-review-round implementation history for parsing/generation bugfixes that never got a permanent **spec-level** regression fixture (as opposed to a Rust-crate-only unit/integration test), and authored the missing ones directly into the spec fixture corpus.

Round 9 (3 findings): R9-F1 (bracket in inline-key position wrongly became `MalformedInlineCompound`/`UnterminatedInlineCompound` instead of `InvalidKey`, fixed in `fea9513`), R9-F2+F3 (ordinary `cargo test` ran load-bearing timing probes on shared global atomics with a real interleaving bug; the R8-F6 index-cost report's "no material cost" verdict was retracted and replaced with an honest 5–37%-on-pathological-shapes / <1%-on-mainstream attribution, fixed in `c7b84fb`). Round 10 (3 findings, all closed): R10-F1 (P2 — `has_quote_bytes` was re-scanning the full inline subtree at every nesting level despite the R9 boundary-memo fix; replaced with a single root-computed `has_quotes` flag threaded down, `acbc34a`), R10-F2 (canonical multiline emitter split-then-rejoined a string instead of one `push_str`, `4a45183`), R10-F3 (I personally corrected the R8-F6 report's attribution arithmetic — it had summed only one of two lookup-counter sites for two shapes — and narrowed an overstated "byte-identical outside cfg(test)" claim to what's actually true, `1b3ceb6`, no sub-agent used). Round 11 (1 finding): R10-F1's root-flag threading reopened an old, previously-unreachable `SplitQ` defect — its `EofAfterWsSkip` handler raised `EmptyKey` for a dotted-key-with-no-separator case instead of deferring to the caller's colon search like `SplitFast` does — fixed in `713a2b0`, and I found a second, independently wrong pre-existing test pin (`tests/inline_trailing_whitespace.rs`) while reviewing the fix.

After round 11, the user asked whether every parsing/generation bugfix gets a spec-level regression fixture, and — after I confirmed most of rounds 1–11 did NOT — authorized a full backfill. This became two paired investigate/author task cycles (`#177`/`#178` for rounds 1–11, `#179`/`#180` for the pre-round-1 implementation-history phase, commit range `5178e11..1881ae6`), both run single-context by me (no `/wrush` — fixture authoring is JSON/`.ktav` file work, not Rust engineering). Method for each: read the finding, reproduce it against the **current, already-fixed** `main` via a disposable probe crate at `D:/system_artefact/Temp/ktav-a2-harness/r11-probe/` (Cargo.toml pointing straight at the primary rust checkout), grep the spec fixture tree for existing coverage, and only author a new `.ktav`+`.json`(+`.canonical.ktav`) fixture when genuinely uncovered. Rounds 1–11 yielded **24 confirmed gaps → 30 fixture files → 10 commits**; the pre-round-1 phase yielded only **2 gaps → 2 fixture files → 2 commits** (that phase was developed alongside the spec corpus itself, so most of its `fix:` commits were closing gaps against fixtures that already existed, not gaps in the corpus).

**A real process mistake, caught and fixed mid-task:** the spec content lives in *two independent git clones* of the same remote — the top-level `D:\dev\ktav-lang\spec` (branch `main`, presumably what the user eventually pushes) and `D:\dev\ktav-lang\rust\spec` (a submodule checkout, detached HEAD, the one `tests/spec_conformance.rs` actually resolves by default with no `KTAV_SPEC_DIR` set). I wrote the first ~24 fixtures only into the top-level checkout, and a `cargo test --test spec_conformance` afterward showed unchanged totals — that's what caught it. Fix: copied every new file into `rust/spec` too and replicated the same commits there (`git show --name-only --format= <sha>` + `git add $files` + `git -C <other-repo> show -s --format=%B <sha> | git commit -q -F -`), then verified `diff -r` between both `versions/0.7/tests` trees is empty. This is now the established pattern for any future spec-fixture work in this ecosystem — check both checkouts, always diff-verify before calling it done.

**A second, purely-mine mistake, also caught before it landed:** while authoring the `01d65ad` gap fixture (an inline Array item written as the escape sequence for `()` must stay literal content, not collapse to the empty-string rule that a plain `()` gets), I typed the literal text `()` instead of the escape sequence `\u0028\u0029` three separate times in a row — once in the `.ktav` fixture file, once via a `printf` attempt that additionally had its own escape-reinterpretation bug, and once in the commit message — without noticing on read-back via `cat`/visual review each time. Caught only because I always verify fixture bytes with `od -c` and re-parse through the probe crate rather than trusting `cat` or my own visual proofreading; fixed the file via `node -e` (building the backslash byte with `String.fromCharCode(92)` to sidestep any further escape-reinterpretation risk) and the commit message via `git commit --amend` (safe — local, unpushed, created seconds earlier, nothing built on top).

**Nothing is in flight.** TaskList is empty (`#177`–`#180` all completed and closed in order); the babysit cron was armed and self-deleted twice this session (once for rounds 9–11, once for the fixture-backfill pair) — no cron is currently running. All work this session stayed local: zero pushes to `rust`, zero pushes to either `spec` clone, `Cargo.toml`/`spec-version` untouched at `0.6.4` throughout.

## Active goal

None. `/goal` was never armed this session; progress was governed by the TaskList plus `/babysit`, both now empty/retired.

## TaskList

Empty — `TaskList` returns "No tasks found".

### recently completed (this session)
- #180 Author spec fixtures for the pre-round-1 gaps found by the audit
- #179 Audit pre-round-1 spec-0.7 bugfix commits (5178e11..1881ae6) for missing spec fixtures
- #178 Author the missing spec fixtures identified by the round 1-11 audit
- #177 Audit rounds 1-11 for normative-behavior findings missing a spec fixture
- #176 R11-F1 (P2): SplitQ's dotted-key EOF handling must match SplitFast's, not short-circuit to EmptyKey
- #175 R10-F3 (P3): fix the index-cost report's attribution arithmetic and provenance claim
- #174 R10-F2 (P3): canonical multiline emitter must not split+rejoin the string
- #173 R10-F1 (P2): stop re-running has_quote_bytes over the whole inline subtree at every depth
- #172 R9-F2+F3 (P2+P2): isolate the load-bearing measurements and make the index A/B honest
- #171 R9-F4 (P3): multiline-form selection must not materialize a Vec of lines (superseded/renumbered mid-session — see #170 below, both closed)

(Earlier: #170 R9-F1 also completed this session, closed before the above.)

No pending, no in_progress, no blocked. No deleted tasks.

## Decisions

- **Extended the fixture-backfill scope to the full pre-round-1 implementation history**, not just the numbered review rounds, per explicit user request ("кажется, за все фикс-сессии нужно завести") — rejected stopping at rounds 1–11 alone once the user clarified they meant *all* bugfix history, not just the review-report series.
- **Treated the two `spec` git clones as requiring independent, byte-identical commits** rather than picking one as authoritative and leaving the other stale — rejected relying on eventually re-pointing the submodule from a push, since that would leave `rust/spec`'s tests silently uncovered by the new fixtures for an indefinite period.
- **Left the `rust` repo's `M spec` submodule-pointer change uncommitted** — rejected committing the pointer bump myself, since that is effectively a version-bump-adjacent decision (matches the existing `chore(deps): bump spec submodule to ...` commit pattern in history) that belongs to the user, not something to do proactively.
- **Verified every fixture's raw bytes with `od -c` and re-parsing through a probe crate, never trusting `cat` or visual proofreading of my own written text** — this is what caught both the `()`/`\u0028\u0029` authoring slip and the two-checkout gap; adopted as a hard rule for all remaining fixture work.
- **Amended the one just-created, unpushed, local commit** whose message had the same escape-sequence typo, rather than leaving it — judged safe specifically because it was local, seconds old, and nothing depended on it yet; did not apply this to the round-4 commit message typo from earlier in the session (already several commits back by the time it was noticed), which was left as a cosmetic-only artifact.

## Open questions

- **Reconciling the two local `spec` clones before any push.** `D:\dev\ktav-lang\spec` (top-level, branch `main`) and `D:\dev\ktav-lang\rust\spec` (submodule, detached HEAD) now have 12 new, identical-content but independently-created commits each, both unpushed. Before either is ever pushed, the user needs to decide which is the source of truth and how the other re-points to it (`.gitmodules` uses a remote GitHub URL, not a local path, so `git submodule update` will not pick up the local clone's commits automatically).
- **Whether/when to commit the `rust` repo's `M spec` submodule-pointer bump.** Left uncommitted this session per the "no version bumps without explicit ask" spirit; the user has not yet said whether to commit it now or wait.
- **Push authorization**, carried over from prior checkpoints and still unresolved: the `rust` repo is now well ahead of `origin/main` (every round 9–11 fix commit plus the fixture-backfill's submodule-pointer question) and neither `push` nor the fixture-backfill commits in either `spec` clone have been sent anywhere.
- **The two minor observations flagged in the 2026-09-08-2116 checkpoint** (an extra `Vec` allocation in `choose_multiline_form` for a since-superseded code path, and a possibly-dead `u64` counter in a release build) were not revisited this session — status unchanged, still open if the user wants them looked at.

## Repo state

**rust** (`D:\dev\ktav-lang\rust`):
```
 M spec
?? docs/checkpoints/2026-09-08-1245.md
?? docs/checkpoints/2026-09-08-2116.md
```
```
713a2b0 fix(inline): SplitQ pushes the EofAfterWsSkip remainder instead of raising EmptyKey
b01a43a Review Rust spec 0.7 implementation, round 11
acbc34a fix(inline): thread root quote-presence; colon scan checks only the key prefix
4a45183 perf(render): stop splitting and rejoining the canonical multiline body
1b3ceb6 docs(review): correct R8-F6's attribution arithmetic and scope claim
```
Full suite at this HEAD: 1079 passed / 3 ignored / 52 suites. `Cargo.toml` version and `package.metadata.ktav.spec-version` both still `0.6.4`.

**spec (top-level, `D:\dev\ktav-lang\spec`, branch `main`)** — HEAD `c349b2e "Add regression fixture for quoted-key brace at nesting depth 3 (8f6a0a1)"`, working tree clean, 12 new local commits this session, none pushed.

**spec (submodule, `D:\dev\ktav-lang\rust\spec`, detached HEAD)** — HEAD `edc6aa9`, same commit content as the top-level clone (`diff -r` between both `versions/0.7/tests` trees is empty), working tree clean, none pushed.

`cargo test --test spec_conformance --nocapture` from the primary rust checkout (default resolution, no `KTAV_SPEC_DIR`): 221 valid / 74 invalid (owned) / 73 invalid (thin) / 5 unrepresentable / 4 parseable-unrepresentable, all 9 test functions passing — up from the session-start baseline of 202/61/60/5/4.
